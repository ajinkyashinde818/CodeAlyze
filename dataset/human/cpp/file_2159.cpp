/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author
 */


#include <iostream>
#include <algorithm>
#include <vector>
#include <string>
#include <queue>
#include <map>
#include <set>
#include <functional>
#include <cmath>
#include <numeric>
#include <iterator>
#include <fstream>

#define SIZE 100005
#define MOD 1000000007

using namespace std;

class DFlippingSigns {
public:
  static void solve(std::istream &cin, std::ostream &cout) {
    int64_t n;
    cin >> n;
    vector<int64_t> vec(n);
    int64_t min_value = INT64_MAX;
    int64_t count = 0;
    for (int i = 0; i < n; ++i) {
      cin >> vec[i];
      if (vec[i] < 0) count++;
      vec[i] = abs(vec[i]);
      min_value =  min(min_value,vec[i]);
    }
    if (count %2 == 1){
      cout << accumulate(vec.begin(), vec.end(), 0ll) - 2*min_value << endl;
    }else{
      cout << accumulate(vec.begin(), vec.end(), 0ll) << endl;
    }
  }
};


int main() {
    DFlippingSigns solver;
    std::istream& in(std::cin);
    std::ostream& out(std::cout);
    solver.solve(in, out);
    return 0;
}
