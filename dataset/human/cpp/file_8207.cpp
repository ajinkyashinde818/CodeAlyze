/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author TM
 */


#include <bits/stdc++.h>

using namespace std;

class CSplittingPile {
public:
    void solve(std::istream &cin, std::ostream &cout) {
        int n;
        cin >> n;
        vector<int> vec(n, 0);
        long long res = 1e+14;
        long long arai = 0;
        long long snuke = 0;

        for (int i = 0; i < n; ++i) {
            cin >> vec[i];
            arai += vec[i];
        }
        for (int j = 0; j < n - 1; ++j) {
            snuke += vec[j];
            arai -= vec[j];
            res = min(abs(arai - snuke), res);
        }
        cout << res << endl;
    }
};


int main() {
    CSplittingPile solver;
    std::istream &in(std::cin);
    std::ostream &out(std::cout);
    solver.solve(in, out);
    return 0;
}
