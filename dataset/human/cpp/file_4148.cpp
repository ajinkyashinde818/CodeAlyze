/* Generated by powerful Codeforces Tool
 * You can download the binary file in here https://github.com/xalanq/cf-tool (Windows, macOS, Linux)
 * Author: alireza_kaviani
 * Time: 2020-05-18 20:41:30
**/
#include <bits/stdc++.h>
#include <ext/pb_ds/tree_policy.hpp>
#include <ext/pb_ds/assoc_container.hpp>

using namespace std;
using namespace __gnu_pbds;

template <class T> using Tree = tree<T, null_type, less<T>, rb_tree_tag,tree_order_statistics_node_update>;

typedef long long ll;
typedef long double ld;
typedef pair<ll, ll> pll;

#define all(x)                      (x).begin(),(x).end()
#define Sort(x)                     sort(all((x)))
#define X                           first
#define Y                           second
#define sep                         ' '
#define endl                        '\n'
#define SZ(x)                       ll(x.size())

ll poww(ll a, ll b, ll md) {
    return (!b ? 1 : (b & 1 ? a * poww(a * a % md, b / 2, md) % md : poww(a * a % md, b / 2, md) % md));
}

const ll MAXN = 1e3 + 10;
const ll LOG = 22;
const ll INF = 8e18;
const ll MOD = 1e9 + 7; // 998244353; // 1e9 + 9;

ll n , xs , ys , xt , yt , mark[MAXN] , x[MAXN] , y[MAXN] , r[MAXN];
double dist[MAXN] , D[MAXN][MAXN];
vector<pair<pll , ll>> C;
vector<pair<ll , double>> adj[MAXN];

int main() {
    ios::sync_with_stdio(false);cin.tie(0);cout.tie(0);

    cin >> xs >> ys >> xt >> yt >> n;
    C.push_back({{xs , ys} , 0});
    C.push_back({{xt , yt} , 0});
    for(int i = 0 ; i < n ; i++)    cin >> x[i] >> y[i] >> r[i] , C.push_back({{x[i] , y[i]} , r[i]});
    for(int i = 0 ; i < n + 2 ; i++){
        for(int j = i + 1 ; j < n + 2 ; j++){
            ll ax = C[i].X.X , ay = C[i].X.Y , ar = C[i].Y;
            ll bx = C[j].X.X , by = C[j].X.Y , br = C[j].Y;
            double d = sqrt((ax - bx) * (ax - bx) + (ay - by) * (ay - by)) - ar - br;
            d = max(d , double(0));
            D[i][j] = D[j][i] = d;
        }
    }
    
    fill(dist , dist + MAXN , INF) ; dist[0] = 0;
    for(int i = 0 ; i < n + 2 ; i++){
        int v = MAXN - 1;
        for(int j = 0 ; j < n + 2 ; j++)    if(!mark[j] && dist[j] < dist[v])   v = j;
        mark[v] = 1;
        for(int j = 0 ; j < n + 2 ; j++){
            if(dist[j] > dist[v] + D[v][j]){
                dist[j] = dist[v] + D[v][j];
            }
        }
    }
    printf("%.10f" , dist[1]);

    return 0;
}
/*

*/
