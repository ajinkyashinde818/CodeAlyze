/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author yassin
 */

#include <bits/stdc++.h>

using namespace std;

class tenka1_2018_d {
public:
    void solve(istream &in, ostream &out) {
        int N;
        in >> N;
        for (int m = 1; m * (m + 1) / 2 <= N; ++m) {
            if (m * (m + 1) / 2 == N) {
                out << "Yes\n" << m + 1 << "\n";
                vector<set<int>> res(m + 1);
                out << m << " ";
                for (int i = 1; i <= m; ++i) {
                    res[0].insert(i);
                    out << i << " ";
                }
                out << "\n";
                int upto = m;
                for (int i = 1; i < m + 1; ++i) {
                    for (int j = 0; j < i; ++j) {
                        res[i].insert(*res[j].begin());
                        res[j].erase(res[j].begin());
                    }
                    while ((int)res[i].size() < m) {
                        res[i].insert(++upto);
                    }
                    out << m << " ";
                    for (int x : res[i]) {
                        out << x << " ";
                    }
                    out << "\n";
                    for (int j = 0; j < i; ++j) {
                        res[i].erase(res[i].begin());
                    }
                }
                return;
            }
        }
        out << "No\n";
    }
};

int main() {
	std::ios::sync_with_stdio(false); cin.tie(nullptr);
	tenka1_2018_d solver;
	std::istream& in(std::cin);
	std::ostream& out(std::cout);
	solver.solve(in, out);
	return 0;
}
