import math, algorithm, tables, sets, lists, queues, intsets, macros, critbits, sequtils, strutils, future

var s: string
s = readLine(stdin)

var a,b,c = 0

if s.find('a') == -1 or s.find('b') == -1 or s.find("c") == -1:
    echo "No"
else:
    echo "Yes"
