#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int n;
	// ??????????????°n???????????????
	scanf("%d", &n);

	int *ip;
	// ???????????¨????????????????????????????????????????????????????¢????
	ip = (int *)malloc(n * sizeof(int));

	int i, j;
	int temp;
	// ?¶??????????n?????????????????\??????????????????
	for (i=0; i<n; i++) {
		scanf("%d", &ip[i]);
	}

	/*
	//???????????????needs improvement in time-efficiency
	int max = ip[1]-ip[0];
	for (i=0; i<n; i++) {
		for (j=i+1; j<n; j++) {
			if (ip[j] - ip[i] > max) {
				max = ip[j] - ip[i];
			}
		}
	}
	printf("%d\n", max);
	*/

	int max_difference = ip[1]-ip[0];
	int min_idx = 0;
	for (i=1; i<n; i++) {
		if (ip[i]-ip[min_idx] > max_difference) {
			max_difference = ip[i]-ip[min_idx];
		}
		if (ip[i] < ip[min_idx]) {
			min_idx = i;
		}
	}
	printf("%d\n", max_difference);

	free(ip);
	return 0;
}
