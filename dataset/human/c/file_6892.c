#include <stdio.h>
#include <algorithm>
#include <vector>
#include <string>
#include <queue>
#include <math.h>
const long long mod = 1000000007;
//const long long mod = 998244353;
int main() {
	int N,M;
	scanf("%d", &N);
	scanf("%d", &M);
	printf(M < N ? "No" : "Yes");
	return 0;
}
