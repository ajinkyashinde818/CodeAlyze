#include <stdio.h>
#include <math.h>
#include <string.h>

int main(){
	int n, m;
	if(scanf("%d", &n)== 1);
	if(scanf("%d", &m)== 1);
	
	if(n == m){
		printf("Yes");
	}else{
		printf("No");
	}
	
	
    return 0;
}
