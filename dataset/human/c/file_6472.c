#include <stdio.h>

int main(void) {
	int i; //??????????????°
	int n; //???????????°
	int x1, x; //1???????????°???????????\????????°
	int minv, maxv; //????°????????????§??????

	scanf("%d", &n);

	scanf("%d", &x1);

	minv = x1;
	maxv = -1000000000;

	for(i = 1; i < n; i++) {
		scanf("%d", &x);
		if(x - minv > maxv) {
			maxv = x - minv;
		}
		if(x < minv) {
			minv = x;
		}
	}

	printf("%d\n", maxv);

	return 0;
}
