#include <stdio.h>
#include <math.h>
#include <string.h>

int main(void)
{
  int N, M, K, i, j, k, tmp, X[100001], ans;
  double S;
  scanf("%d %d", &N, &M);
  if (N == M)
  {
    printf("Yes\n");
  }
  else
  {
    printf("No\n");
  }
  return 0;
}
